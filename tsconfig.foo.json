{
  "extends": "./tsconfig.json",

  // `"prisma/runtime.d.ts"` is temporary until
  // https://github.com/prisma/prisma/issues/27027 is fixed

  //"include": ["src/**/*", "prisma/runtime.d.ts"],

  "include": ["src/**/*"],

  "compilerOptions": {
    "composite": true,
    "module": "nodenext",
    "moduleResolution": "nodenext"
  }
}
